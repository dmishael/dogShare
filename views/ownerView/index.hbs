<h1>appointments</h1>
<a href="/appform"><button>make appointment</button></a>

{{#each appointment}}

                {{startDate}}
                {{endDate}}
                {{owner}}
                {{dog}}
                <p>{{sitterComment}}</p>


                <a href="{{_id}}/appointment"><button>Sign Up</button> </a>
                {{!-- //the id is referred to by the handlebars appointment id --}}
                <div>
                <form action="/apps/{{_id}}?_method=DELETE" method="POST">
                <button type="submit" class="btn"> Delete </button></form>
                <a href="/{{_id}}/appointment"><button>Edit</button> </a>
                </div>

                

{{/each}}